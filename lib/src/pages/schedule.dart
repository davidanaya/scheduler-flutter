import 'package:flutter/material.dart';

class Schedule extends StatelessWidget {
  static const routeName = "/schedule";

  @override
  Widget build(BuildContext context) {
    return Container(child: Text('Schedule'));
  }
}

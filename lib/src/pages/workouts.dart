import 'package:flutter/material.dart';

class Workouts extends StatelessWidget {
  static const routeName = "/workouts";

  @override
  Widget build(BuildContext context) {
    return Container(child: Text('Workouts'));
  }
}
